name: sdkman-java
description: OpenJDK installed via SDKMAN!

requires:
  - sdkman

parameters:
  version:
    type: string
    default: "25-open"
    description: "SDKMAN! Java version identifier (e.g. 25-open, 21-open, 17.0.13-tem)"

run_as_user:
  - |
    source ~/.sdkman/bin/sdkman-init.sh
    sdk install java ${param_version}

verify: test -x /home/agentuser/.sdkman/candidates/java/current/bin/java
