name: sdkman-quarkus
description: Quarkus CLI installed via SDKMAN!

requires:
  - sdkman

run_as_user:
  - |
    source ~/.sdkman/bin/sdkman-init.sh
    sdk install quarkus

verify: test -x /home/agentuser/.sdkman/candidates/quarkus/current/bin/quarkus
