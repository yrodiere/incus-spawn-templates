name: firewalld
description: Firewall with permissive defaults for container networking

packages:
  - firewalld

run:
  - systemctl enable --now firewalld
  - firewall-cmd --set-default-zone=trusted
